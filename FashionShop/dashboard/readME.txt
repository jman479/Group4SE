/*Dashboard configuration*/
1. Check for any existing package-lock.json file and /node_modules/ directories and DELETE them.
   (hint, the /node_modules/ will be a hidden folder, so make sure you have enabled see all files).
2. Type the commands below
    -   npm install 
    -   npm start
